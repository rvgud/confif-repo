spring:
      application:
        name: department-service
      jpa:
      show-sql: true
      h2:
      console:
      enabled: true

eureka:
      client:
      service-url:
      default-zone: http://localhost:8761/eureka

      
